# Google Keep clone built using React
## How to Run:
Currently there is an issue with node.js v17 that requires you to run this command:
    export NODE_OPTIONS=--openssl-legacy-provider

After, simply run:
    npm start 
